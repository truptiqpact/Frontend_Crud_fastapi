// [Not used] This project authenticates against the FastAPI backend with JWT
// (see src/services/api/axios.js and src/features/auth). Firebase is not wired in.
// Kept as an empty stub so the agreed folder structure stays intact.
export default null
